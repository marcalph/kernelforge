# Kernelforge

This repo serves as a minified compilation of experiments regarding ML performance.  
It covers profiling and leverage CUDA kernels.

## when using lamdda labs
`sudo usermod -aG docker $USER && newgrp docker`
